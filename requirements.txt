# 安装前必读：
#   1. Python 用 3.10-3.12（推荐 3.12），OCR 依赖暂无 3.13 的安装包
#   2. 先建并激活虚拟环境：py -3.12 -m venv .venv 然后 .venv\Scripts\activate
#   3. 国内装加清华镜像（否则常超时）：
#      pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# PDF 渲染
pymupdf>=1.24
# OCR（纯 ONNX，离线可用）
rapidocr-onnxruntime>=1.3.0
# 表格结构识别（有线表 / 无线表，输出带合并单元格的 HTML；仅作回退方案）
wired-table-rec>=0.0.5
lineless-table-rec>=0.0.5
# HTML 表格解析
beautifulsoup4>=4.12
lxml>=4.9
# Excel
openpyxl>=3.1
numpy>=1.24
