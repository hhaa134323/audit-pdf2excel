# PDF 渲染
pymupdf>=1.24
# OCR（纯 ONNX，离线可用）
rapidocr-onnxruntime>=1.3.0
# 表格结构识别（有线表 / 无线表，输出带合并单元格的 HTML）
wired-table-rec>=0.0.5
lineless-table-rec>=0.0.5
# HTML 表格解析
beautifulsoup4>=4.12
lxml>=4.9
# Excel
openpyxl>=3.1
numpy>=1.24
